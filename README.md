To everyone forking this!! In android studio, open to the Calendar folder, not the root. Otherwise gradle won't build properly

Also, if prompted, don't update gradle. Seems like a good idea, but don't
